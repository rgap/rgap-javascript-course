// Example 3: Focused on the "Why" (Minimal Stating of the Obvious)

// Generate squares of the first 10 natural numbers.
let squares = [];
for (let x = 0; x < 10; x++) {
  squares.push(x * x);
}

// Log the squares array to verify correctness.
console.log(squares);
