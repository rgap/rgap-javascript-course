// greet.js
export function greet(name) {
  return `Hello, ${name}!`;
}

export function greetFormally(title, name) {
  return `Good day, ${title} ${name}.`;
}

/*
Explanation:
- This file defines functions using named exports.
- The `greet` and `greetFormally` functions are exported from this module.
*/
