// greet.js (ES6 Module)
export function greet(name) {
  return `Hello, ${name}!`;
}

/*
Explanation:
- This file defines a module using ES6 syntax.
- The `export` keyword is used to export the `greet` function so that it can be imported in other files.
*/
