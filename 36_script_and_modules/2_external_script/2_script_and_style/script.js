// Now that the DOM is fully loaded, we can safely access the element.
var element = document.getElementById("myElement");
// Log the element's content to the console.
console.log("Element from external script:", element.innerHTML); // Expected output: "Hello from the DOM!"
